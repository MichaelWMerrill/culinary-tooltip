# dependencies
node_modules/

# Astro cache
.astro/

# NOTE: dist/ is intentionally committed so Cloudflare Workers deploys the
# pre-built static assets directly (no build step runs in the deploy env).
# Rebuild with `npm run build` and commit dist/ after changing anything in src/.

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example
